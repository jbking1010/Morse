<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joelbaumann.morsetest">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--<activity android:name=".MainActivity">

        </activity>-->
        <activity android:name=".TranslateOptions"/>
        <activity android:name=".Settings"/>
        <activity android:name=".Communicate"/>
        <activity android:name=".Send"/>
        <activity android:name=".Receive"/>
        <activity android:name=".ToText"/>
        <activity android:name=".ToMorse"/>
        <activity
            android:name=".Home"
            android:label="Home"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>